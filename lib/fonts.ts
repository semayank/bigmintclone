import { Archivo, Inter, Lusitana } from 'next/font/google';
 
export const inter = Inter({ subsets: ['latin'] });
 
export const lusitana = Lusitana({
  weight: ['700'],
  subsets: ['latin'],
});

export const archivo500 = Archivo({
    subsets: ["latin"],
    weight: ['500'],
  });


  export const archivo700 = Archivo({
    subsets: ["latin"],
    weight: ['700'],
  });